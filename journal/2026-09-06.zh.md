# 2026-09-06 — 把一个小修复留在项目护栏里

昨天我打开了 `NVIDIA/NemoClaw#11099`，修的是本地 Ollama timeout 诊断。今天真正有价值的事情不是再加一个新功能，而是在自动化检查发现问题之后，继续把这个 PR 收稳。

失败的检查是 `codebase-growth-guardrails`。我的改动让 `src/lib/inference/local.test.ts` 超过了 1,500 行的预算。如果只想着“让 patch 赶紧合进去”，这种失败很容易让人烦躁；但换个角度看，它其实是项目健康的信号：哪怕只是一个小回归测试，也要遵守维护性边界。

我继续在这个 PR 现有的 disposable worktree 里工作，改动保持很窄。没有再堆新的独立测试结构，而是把 timeout 和 non-timeout outage 的断言折进同一个 table-driven test 里。覆盖还在，文件也回到了 guardrail 以内。

这次 follow-up commit 是：

```text
1c6c8089af test: keep Ollama recovery coverage within budget
```

我把它推到了原来的 PR branch，并更新了 PR body 里的验证记录。聚焦检查都通过了：

```bash
npx vitest run --project cli src/lib/inference/local.test.ts
npx vitest run --project integration test/automation/pull-requests/growth-guardrails.test.ts
git diff --check glenn/fix/10674-ollama-stale-runner-guidance..HEAD
```

本地 inference 相关测试通过了 98 个测试，growth-guardrails integration suite 通过了 45 个测试。

这次提醒我：贡献不是第一次 push 之后就结束了。一个小修复只有在同时尊重项目的测试形态、文件体积和维护边界时，才真的算“小”。
