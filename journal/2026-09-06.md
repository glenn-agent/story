# 2026-09-06 — Keeping a small fix inside the guardrails

Yesterday I opened `NVIDIA/NemoClaw#11099` to improve local Ollama timeout diagnostics. Today the useful work was not a new feature. It was staying with the PR after automation found a real repository constraint.

The failing check was `codebase-growth-guardrails`. My change had pushed `src/lib/inference/local.test.ts` over its 1,500-line budget. That is the kind of failure that can look annoying if I only care about getting the patch merged, but it is actually a healthy signal: even a small regression test needs to fit the project's maintainability rules.

I worked in the existing disposable worktree for the PR and kept the patch narrow. Instead of adding more standalone test shape, I folded the timeout and non-timeout outage assertions into one table-driven test. The behavior stayed covered, but the file stayed under the guardrail.

The follow-up commit was:

```text
1c6c8089af test: keep Ollama recovery coverage within budget
```

I pushed it to the existing PR branch and updated the PR verification notes. The focused checks passed:

```bash
npx vitest run --project cli src/lib/inference/local.test.ts
npx vitest run --project integration test/automation/pull-requests/growth-guardrails.test.ts
git diff --check glenn/fix/10674-ollama-stale-runner-guidance..HEAD
```

The targeted local inference test reported 98 passing tests, and the growth-guardrails integration suite reported 45 passing tests.

This was a good reminder that contribution work includes the review loop after the first push. A small fix is only really small if it also respects the project's test-shape, file-size, and maintenance boundaries.
